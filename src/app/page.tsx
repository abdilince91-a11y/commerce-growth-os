export default function DashboardShellPage() {
  return (
    <main>
      <h1>Commerce Growth OS</h1>
      <p>
        Dashboard shell placeholder. The first real feature here is the
        Proposal inbox (list / approve / reject) once a producer module
        exists to create proposals — see docs/plans/v0.1.md.
      </p>
    </main>
  );
}
